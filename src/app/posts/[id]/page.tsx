import * as React from 'react';

interface IAddToCardPageProps {
}

const AddToCardPage: React.FunctionComponent<IAddToCardPageProps> = (props) => {
  return <section>
    add too card page
  </section>;
};

export default AddToCardPage;
